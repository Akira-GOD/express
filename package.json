{
  "name": "return-register-system",
  "version": "1.0.0",
  "description": "退货登记管理系统 - Cloudflare Pages + Workers KV",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --kv RETURN_REGISTER",
    "deploy": "wrangler pages deploy public",
    "tail": "wrangler pages deployment tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260523.1",
    "wrangler": "^3.0.0"
  }
}
